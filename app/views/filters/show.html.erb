

<h1><%= link_to @filter.collection.title, @filter.collection %></h1> 


<h3><%= @filter.name %></h3>


<%= @filter.tag %>
<%= link_to 'Delete', @filter, method: :delete, data: { confirm: 'Are you sure?' } %>
<%# @filter.collection.title %>